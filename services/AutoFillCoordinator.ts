/**
 * @deprecated Decommissioned in Phase 6.
 * Automatic TrackPlayer queue auto-filling has been removed in favor of
 * UI-only Up Next discovery recommendations managed directly by usePlayerStore
 * and RecommendationEngine without mutating the native TrackPlayer queue.
 */
export {};
